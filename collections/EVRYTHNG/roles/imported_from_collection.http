#!/usr/bin/env dothttp

# imported from 35240-3abc9a78-733b-4c0c-8e67-86b81dc5a077.postman_collection.json

// Create a new Application User role. Only the account owner can create new roles. You cannot create roles with the same `name` as the predefined `admin` and `none` roles. Roles `name`s are considered unique. This means two roles with different `id`s cannot share the same `name`.

@name("Create an Application User role")
POST "https://{{baseUrl}}/roles"
? "No Name"= "<string>"



